import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.psyke',
  appName: 'PsykeFolio',
  webDir: 'dist/psyke-folio',
  bundledWebRuntime: false
};

export default config;
